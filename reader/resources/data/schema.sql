CREATE TABLE IF NOT EXISTS ROUTE (
    IDT_ROUTE SERIAL NOT NULL,
    NAM_DESTINARION VARCHAR NOT NULL,
    NAM_SHORT VARCHAR NOT NULL UNIQUE,
    DAT_CREATED_AT TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    CONSTRAINT ROUTE_PK PRIMARY KEY (IDT_ROUTE)
);